import { Box, Typography } from '@mui/material';
import React from 'react';

const Properties = () => {
    return (
        <Box sx={{color:'#000',height:'100vh'}}>
            <Typography>Hello property</Typography>
        </Box>
    );
};

export default Properties;