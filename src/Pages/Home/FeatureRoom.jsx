import React from 'react';

const FeatureRoom = ({ feature }) => {
     const { id, featured, authorThumb, authorName, status, propertyName, propertyDesciption, startFrom, beds, baths, sqFt, saved } = feature
     return (
          <div>

          </div>
     );
};

export default FeatureRoom;